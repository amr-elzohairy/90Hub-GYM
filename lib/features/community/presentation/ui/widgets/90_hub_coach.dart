import 'package:flutter/material.dart';

class HubCoach90 extends StatelessWidget {
  const HubCoach90({super.key});

  @override
  Widget build(BuildContext context) {
    return Align(
      alignment: Alignment.centerLeft,
      child: Image.asset(
        "assets/images/90hub-gym-with-text-logo.png",
        height: 55,
      ),
    );
  }
}
